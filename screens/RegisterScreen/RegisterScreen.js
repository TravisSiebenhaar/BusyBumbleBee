import React from "react";
import { SafeAreaView, View, Text } from "react-native";

const RegisterScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>This is registration screen page</Text>
      </View>
    </SafeAreaView>
  );
};

export default RegisterScreen;
